export interface Position {
    productId: string;
    quantity: number;
    positionId: string;
    x: number;
    y: number;
    z: number;
  }
  
export interface Order {
    productId: string;
    quantity: number;
}